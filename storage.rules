// O projeto foi ajustado para salvar anexos pequenos em Base64 no Firestore.
// Firebase Storage não é necessário no plano gratuito.
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
